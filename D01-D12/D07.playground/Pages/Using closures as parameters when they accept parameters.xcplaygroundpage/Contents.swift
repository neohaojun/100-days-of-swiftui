import UIKit

// travel() function that accepts a closure as its only parameter, and that closure in turn accepts a string
func travel(action: (String) -> Void) {
    print("I'm getting ready to go.")
    action("London")
    print("I arrived!")
}

travel { (place: String) in
    print("I'm going to \(place) in my car")
}
