import UIKit

// this effectively creates a function without a name, and assigns it to driving
let driving = {
    print("I'm driving in my car")
}

// you can now call driving() as if it were a regular function
driving()
